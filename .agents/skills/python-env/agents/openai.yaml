interface:
  display_name: "Python Environment"
  short_description: "Use the project Python environment correctly"
  default_prompt: "Use $python-env to run a Python command in the correct project environment."
