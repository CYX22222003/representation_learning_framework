interface:
  display_name: "Project Plan"
  short_description: "Assess progress and choose next actions"
  default_prompt: "Use $project-plan to summarize current progress and identify the next phase-based actions."
