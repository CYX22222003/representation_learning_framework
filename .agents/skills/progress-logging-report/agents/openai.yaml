interface:
  display_name: "Progress Logging Report"
  short_description: "Generate concise supervisor-facing project progress logs."
  default_prompt: "Create a dated progress logging report from the current schedule, plan, research writeup, recent commits, and artifact evidence."
