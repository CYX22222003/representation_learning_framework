interface:
  display_name: "Sync Project Docs"
  short_description: "Keep project design documents consistent"
  default_prompt: "Use $sync-docs to propagate the current design change across all affected project documents."
